{
  "name": "mozart-a11y",
  "version": "1.0.0",
  "description": "Mozart & Co self-hosted Liquid Glass accessibility widget. Self-injecting, single-file. Replaces UserWay on Webflow client sites.",
  "private": true,
  "scripts": {
    "build": "esbuild src/mozart-a11y.js --minify --legal-comments=inline --target=es2018 --outfile=dist/mozart-a11y.min.js",
    "test": "node test/verify.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "playwright": "^1.49.0"
  }
}
