{
  "name": "mozart-a11y",
  "version": "1.0.0",
  "description": "Mozart & Co self-hosted Liquid Glass accessibility widget. Self-injecting, single-file. Replaces UserWay on Webflow client sites.",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "node test/verify.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "playwright": "^1.49.0"
  }
}
